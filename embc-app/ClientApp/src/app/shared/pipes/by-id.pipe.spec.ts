/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ByIdPipe } from './by-id.pipe';

describe('Pipe: ById', () => {
  it('create an instance', () => {
    let pipe = new ByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
