/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PluckPipe } from './pluck.pipe';

describe('Pipe: Pluck', () => {
  it('create an instance', () => {
    let pipe = new PluckPipe();
    expect(pipe).toBeTruthy();
  });
});
