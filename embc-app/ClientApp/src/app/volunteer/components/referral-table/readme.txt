This component should display a table of referrals.
  Each table row should have a checkbox to select the row.
  Each table row should have Referral #, SUPPLIER NAME, VALID FROM, VALID TO, TYPE and ACTION
  Each table row should have a View action that routes to the View Referral page.
If there are 0 referrals then instead of table, display "No referrals have been added yet".
If there are > 0 selected referrals then display PRINT SELECTED REFERRALS button.
The PRINT SELECTED REFERRALS button should ...
